/**\n * errorNormalizer.js\n * \n * Converts various error types into a standard normalized format\n * Prevents framework/internal error leakage to clients\n */\n\nconst HTTP_STATUS_MAP = {\n  // Client errors\n  'BAD_REQUEST': 400,\n  'UNAUTHORIZED': 401,\n  'FORBIDDEN': 403,\n  'NOT_FOUND': 404,\n  'CONFLICT': 409,\n  'VALIDATION_ERROR': 400,\n  'RATE_LIMITED': 429,\n  \n  // Server errors\n  'INTERNAL_ERROR': 500,\n  'SERVICE_UNAVAILABLE': 503,\n  'TIMEOUT': 504,\n};\n\nconst ERROR_CODE_MAP = {\n  // MongoDB errors\n  'MongoNetworkError': {\n    code: 'DATABASE_ERROR',\n    status: 503,\n    message: 'Database connection error. Please try again.',\n  },\n  'ValidationError': {\n    code: 'VALIDATION_ERROR',\n    status: 400,\n    message: 'Validation failed. Please check your input.',\n  },\n  'CastError': {\n    code: 'INVALID_ID',\n    status: 400,\n    message: 'Invalid ID format.',\n  },\n  'DuplicateKeyError': {\n    code: 'CONFLICT',\n    status: 409,\n    message: 'Resource already exists.',\n  },\n  \n  // JWT errors\n  'JsonWebTokenError': {\n    code: 'INVALID_TOKEN',\n    status: 401,\n    message: 'Invalid authentication token.',\n  },\n  'TokenExpiredError': {\n    code: 'TOKEN_EXPIRED',\n    status: 401,\n    message: 'Authentication token has expired.',\n  },\n  \n  // Axios errors\n  'AxiosError': {\n    code: 'API_ERROR',\n    status: 500,\n    message: 'External service error. Please try again.',\n  },\n  \n  // Network errors\n  'ECONNREFUSED': {\n    code: 'CONNECTION_REFUSED',\n    status: 503,\n    message: 'Service unavailable. Please try again.',\n  },\n  'ENOTFOUND': {\n    code: 'DNS_ERROR',\n    status: 503,\n    message: 'Service unavailable. Please try again.',\n  },\n  'ETIMEDOUT': {\n    code: 'TIMEOUT',\n    status: 504,\n    message: 'Request timeout. Please try again.',\n  },\n};\n\n/**\n * Normalize any error into a standard format\n */\nfunction normalizeError(err) {\n  const errorId = `ERR_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;\n\n  // Already normalized\n  if (err.code && err.status && err.message) {\n    return {\n      ...err,\n      errorId,\n      stack: err.stack,\n    };\n  }\n\n  // Check if it's a known error type\n  const errorName = err.name || err.constructor.name;\n  if (ERROR_CODE_MAP[errorName]) {\n    const config = ERROR_CODE_MAP[errorName];\n    return {\n      code: config.code,\n      status: config.status,\n      message: config.message,\n      errorId,\n      stack: err.stack,\n      originalError: process.env.NODE_ENV === 'development' ? err.message : undefined,\n    };\n  }\n\n  // Check for HTTP error with status code\n  if (err.status || err.statusCode) {\n    const status = err.status || err.statusCode;\n    const code = Object.entries(HTTP_STATUS_MAP).find(([_, s]) => s === status)?.[0] || 'INTERNAL_ERROR';\n    \n    return {\n      code,\n      status,\n      message: err.message || `HTTP ${status}`,\n      errorId,\n      stack: err.stack,\n    };\n  }\n\n  // Check for MongoDB specific errors\n  if (err.name === 'MongoError' || err.name === 'MongooseError') {\n    return {\n      code: 'DATABASE_ERROR',\n      status: 503,\n      message: 'Database error. Please try again.',\n      errorId,\n      stack: err.stack,\n    };\n  }\n\n  // Check for rate limiting\n  if (err.message && err.message.includes('rate limit')) {\n    return {\n      code: 'RATE_LIMITED',\n      status: 429,\n      message: 'Too many requests. Please try again later.',\n      errorId,\n      stack: err.stack,\n    };\n  }\n\n  // Check for timeout\n  if (err.message && (err.message.includes('timeout') || err.message.includes('ECONNABORTED'))) {\n    return {\n      code: 'TIMEOUT',\n      status: 504,\n      message: 'Request timeout. Please try again.',\n      errorId,\n      stack: err.stack,\n    };\n  }\n\n  // Default: generic server error\n  return {\n    code: 'INTERNAL_ERROR',\n    status: 500,\n    message: 'An unexpected error occurred. Please try again.',\n    errorId,\n    stack: err.stack,\n    originalError: process.env.NODE_ENV === 'development' ? err.message : undefined,\n  };\n}\n\n/**\n * Create an application error with standard format\n */\nfunction createError(code, message, status) {\n  const error = new Error(message);\n  error.code = code;\n  error.status = status || HTTP_STATUS_MAP[code] || 500;\n  error.errorId = `ERR_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;\n  return error;\n}\n\nmodule.exports = {\n  normalizeError,\n  createError,\n  HTTP_STATUS_MAP,\n  ERROR_CODE_MAP,\n};\n